// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebaseConfig: {
        apiKey: "AIzaSyAvNgjdmMUbSMta4lNci-NExTxVVu1A9zM",
        authDomain: "portauthar528folio.firebaseapp.com",
        projectId: "portauthar528folio",
        storageBucket: "portauthar528folio.appspot.com",
        messagingSenderId: "356031066929",
        appId: "1:356031066929:web:41572ffe4e1c77f2bbf610",
        measurementId: "G-4CBMKJBMNT"
      }
  };
  
  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/plugins/zone-error';  // Included with Angular CLI.